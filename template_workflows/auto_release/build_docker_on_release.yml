name: build Docker image on release

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: {{ synced_repo.test_username_unquoted }}
          password: {{ synced_repo.test_username_github }}
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: {{ synced_repo.test_username_quoted }}/{{ synced_repo.docker_repo_name }}:latest, {{ synced_repo.test_username_template }}/{{ synced_repo.docker_repo_name }}:${{ github.event.release.tag_name }}